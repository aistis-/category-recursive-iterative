{% extends '::base.html.twig' %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block content %}
    <div class="col-xs-6">
        {{ categoryRecursively(categoryTree)|raw }}
    </div>

    <div class="col-xs-6">
        {{ categoryIteratively(categoryTree)|raw }}
    </div>

    <div class="col-xs-12">
        <h2>{{ 'add_new_category'|trans }}</h2>

        {{ form(form) }}
    </div>

{% endblock %}
